using System;

namespace Distinction_Task
{
    public enum ButtonType {
        ExitButton,
        ResetButton,
        SinglePlayerButton,
        MultiplayerButton,
        MainMenuExitButton
    }
}