using System;

namespace Distinction_Task
{
    public enum ProgramFlow {
        ActionComplete,
        Repeat,
        Exit,
        GameEndsPlayerWon,
        Unassigned,
        StartGame,
        Reset,
        ShowMainMenu,
        CloseGame
    }
}